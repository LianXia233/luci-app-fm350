# Rust 构建产物（本机 target 约 258 MB，绝不入库）
/rust/target/

# OpenWrt 打包产物
*.apk
*.ipk
*.o
*.so

# 发布流水线产物（scripts/build-release.sh 的输出目录）
/dist-release/

# 本地 SDK 工作目录（若把 FM350_WORK_DIR 指向仓库内）
/.sdk-cache/

# 编辑器与系统
.DS_Store
Thumbs.db
*.swp
*~

# 本地临时文件
/tmp/
*.log
